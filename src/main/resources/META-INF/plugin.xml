<!-- Copyright 2000-2023 JetBrains s.r.o. and other contributors. Use of this source code is governed by the Apache 2.0 license that can be found in the LICENSE file. -->
<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->

<idea-plugin>
    <idea-version
            since-build="251"
           />
  <!-- Unique id for this plugin. Must stay constant for the life of the plugin. -->
  <id>org.giuantomcat</id>

  <!-- Text to display as name on Settings | Plugin page -->
  <name>Giuan Tomcat</name>

  <!-- Product and plugin compatibility requirements -->
  <depends>com.intellij.modules.platform</depends>
  <depends>com.intellij.modules.java</depends>

  <!-- Text to display as description on Settings | Plugin page -->
  <description>
    <![CDATA[
      <h3>Run Apache Tomcat directly from IntelliJ IDEA</h3>
      <p>
        "Giuan Tomcat" adds a <b>Giuan Tomcat</b> run configuration that starts an Apache
        Tomcat instance without requiring the bundled Tomcat integration or any external
        plugin: the plugin <b>auto-generates the CATALINA_BASE</b> (conf, logs, work, temp,
        webapps) in the system temp folder, writes <code>server.xml</code> and the
        per-application <code>context.xml</code>, and launches <code>Bootstrap</code> with
        full native IntelliJ debug support (breakpoints, variables, stack traces). No WAR is
        assembled: the web-content directory you choose is used directly as the
        <code>docBase</code>, and classes/libraries are attached through the generated
        <code>context.xml</code>.
      </p>
      <p>
        Works with any Apache Tomcat that keeps the standard layout
        (<code>bin/bootstrap.jar</code> + <code>bin/tomcat-juli.jar</code>): Tomcat 7.x,
        8.x/8.5 and 9.x (the version the plugin is developed and tested against). Tomcat 10+
        is compatible too, provided your web application uses the Jakarta EE namespace and a
        suitable JDK (Java 11+).
      </p>
      <ul>
        <li><b>Class &amp; library consolidation (giuan-merged)</b> — for each run the plugin
          builds a merged classpath under
          <code>&lt;tmp&gt;/giuan-tomcat/&lt;hash&gt;/giuan-merged</code>: the
          <code>target/classes</code> of the selected modules are merged into a single
          <code>WEB-INF/classes</code> and their compile-scope dependency jars are gathered into
          <code>WEB-INF/lib</code>. Class files and jars are exposed via <b>hard links</b>, and
          whole class/resource subtrees that are unique to a single module are linked directly as a
          directory junction/symlink; folders shared by several modules (e.g. the same package
          present in different <code>target/classes</code>) are merged into one real directory. Only
          when the filesystem does not allow linking does the plugin fall back to a physical copy.
          Because links share the underlying files, a recompile is immediately visible to the running
          server. The merge is
          <b>incremental</b>: a manifest remembers the previous state so unchanged modules and jars
          are not relinked on the next run.</li>
        <li><b>No build/deploy step</b> — the consolidated <code>WEB-INF/classes</code> and
          <code>WEB-INF/lib</code> are mounted on the web app through Tomcat
          <code>PreResources</code> (DirResourceSet) at <code>/WEB-INF/classes</code> and
          <code>/WEB-INF/lib</code>, so nothing is copied into your web content.</li>
        <li><b>Startup scan skip (granular, per jar)</b> — the classpath panel has three columns
          (Available / Selected modules / Manage skips) letting you choose, for every dependency jar
          of a module, whether it is skipped or not and speed up boot on large applications:
          <ul>
            <li><b>Per dependency jar (Skip TLD / Skip pluggable)</b>: for each jar of the module
              selected in the middle list you can independently decide to skip (<b>skip</b>) or keep
              (<b>non-skip</b>) it in the TLD/taglib scan (<code>tldSkip</code>) or in the
              pluggability/SCI scan (<code>pluggabilitySkip</code>) of the generated
              <code>context.xml</code>. As soon as at least one jar is excluded the plugin sets
              <code>reloadable="false"</code> and <code>containerSciFilter</code> on the context and
              disables <code>scanClassPath</code>, <code>scanBootstrapClassPath</code>,
              <code>scanAllDirectories</code> and <code>scanAllFiles</code>. Non-flagged jars remain
              scanned and every jar stays mounted and usable; jars of the other modules are still
              scanned.</li>
            <li><b>Annotation scan skip (global checkbox, common to all modules)</b>: on run the
              plugin edits the <code>WEB-INF/web.xml</code> of the web content, adding
              <code>metadata-complete="true"</code> to the
              <code>&lt;web-app&gt;</code> element and an empty <code>&lt;absolute-ordering/&gt;</code>.
              What is skipped: Servlet 3.0 annotation scanning
              (<code>@WebServlet</code>/<code>@WebFilter</code>/...) and the discovery and processing
              of web-fragments and ServletContainerInitializer registered in the application jars.
              The additions are tracked with a marker comment so that removing the flag on a later run
              <b>removes exactly what the plugin added</b>; any pre-existing
              <code>metadata-complete</code>/<code>&lt;absolute-ordering&gt;</code> are left
              untouched. Changes are applied idempotently.</li>
          </ul>
        </li>
        <li><b>Simple configuration</b> — choose CATALINA_HOME, web content (docBase),
          context path and HTTP/shutdown ports from a single dialog, plus the modules that
          contribute to the application classpath. The CATALINA_BASE is auto-generated in the system
          temp folder (<code>&lt;tmp&gt;/giuan-tomcat/&lt;hash&gt;/catalina-base</code>, where the
          hash derives from the project location and the configuration name).</li>
        <li><b>Optional HotSwap mode (DCEVM + hotswap-agent)</b> — enable unlimited runtime
          class redefinition (add/remove fields and methods without restarting). It requires a
          JDK patched with DCEVM (see the prerequisites below); the patched JDK is always forced
          when HotSwap is active, in Run and Debug alike: in Debug the IDE debugger performs the
          reload (JDWP), in Run the agent watches <code>target/classes</code> automatically
          (<code>autoHotswap</code>).</li>
        <li><b>Generated instance reuse</b> — each configuration maps to its own folder under the
          system temp dir (hash of project + configuration name, no collisions between projects);
          the instance is kept between runs and can be started again after it is stopped.</li>
      </ul>
      <p>
        HotSwap prerequisites: a JDK patched with DCEVM — per the
        <a href="https://dcevm.github.io/">DCEVM project</a> the supported HotSpot versions are
        7, 8, 9, 10 and 11 (light installers for 8u181, 8u152 and 7u79; Java 11 via
        <a href="https://github.com/TravaOpenJDK/trava-jdk-11-dcevm/releases">Trava OpenJDK</a>,
        which ships with hotswap-agent integrated) — and a <code>hotswap-agent.jar</code>
        downloaded from the
        <a href="https://github.com/HotswapProjects/HotswapAgent/releases">HotswapAgent
        releases</a>. The plugin launches Tomcat with <code>-XXaltjvm=dcevm</code> plus the
        agent. For a normal run (no HotSwap) any JDK works: the plugin uses the project SDK.
      </p>
    ]]>
  </description>
  <change-notes>
    <![CDATA[
      <ul>
        <li><b>2.4.0</b> Class &amp; library consolidation (giuan-merged): merge the selected modules' target/classes and compile dependency jars into a merged WEB-INF/classes and WEB-INF/lib via hard links/junctions/symlinks (deduplicated, incremental via a manifest, copy fallback), then mount them through PreResources in context.xml. Per-jar startup-scan skip now lets you choose skip or non-skip granularly for each dependency.</li>
        <li><b>2.3.0</b> Skip scan: per-module jar-scan skip now also sets reloadable="false", containerSciFilter and disables classpath/bootstrap/all-directory/all-file scanning in context.xml; the global annotation-scan skip also writes &lt;absolute-ordering/&gt; into WEB-INF/web.xml.</li>
        <li><b>2.2.1</b> Replace deprecated LibraryOrSdkOrderEntry.getFiles() with getRootFiles().</li>
        <li><b>2.2.0</b> HotSwap for Java 8 only: a JDK with DCEVM already installed is assumed. Agent configured per the hotswapagent.org guide (-XXaltjvm=dcevm -javaagent:hotswap-agent.jar). Removed the Java version selection and the automatic JDK/DCEVM download.</li>
        <li><b>2.1.0</b> Add optional HotSwap mode (DCEVM + hotswap-agent).</li>
        <li><b>2.0.0</b> Convert to Gradle-based plugin.</li>
        <li><b>1.0.0</b> Release 2018.3 and earlier.</li>
      </ul>
    ]]>
  </change-notes>

  <!-- Text to display as company information on Settings | Plugin page -->
  <vendor url="https://plugins.jetbrains.com">Giuan</vendor>

  <extensions defaultExtensionNs="com.intellij">
    <configurationType implementation="org.giuantomcat.runConfiguration.GiuanTomcatConfigurationType"/>
  </extensions>

</idea-plugin>
